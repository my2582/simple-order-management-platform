#!/bin/bash
# Simple wrapper for listing model portfolios
# Usage: ./list-portfolios [OPTIONS]

cd "$(dirname "$0")" || exit 1
exec python3 -m simple_order_management_platform.cli list-portfolios "$@"