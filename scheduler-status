#!/bin/bash
# Simple wrapper for checking scheduler status
# Usage: ./scheduler-status [OPTIONS]

cd "$(dirname "$0")" || exit 1
exec python3 -m simple_order_management_platform.cli scheduler-status "$@"