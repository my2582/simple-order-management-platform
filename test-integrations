#!/bin/bash
# Simple wrapper for testing all integrations
# Usage: ./test-integrations [OPTIONS]

cd "$(dirname "$0")" || exit 1
exec python3 -m simple_order_management_platform.cli test-integrations "$@"